creating a operating system which uses VRAM to display the content instead of using the BIOS functions

to run the code follow the commands
- go to the project directory
- Run the command to compile the stuff and run the dosbox 
        - ./build.sh
